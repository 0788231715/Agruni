from django.urls import path
from . import views

app_name = "recycling"

urlpatterns = [
    # Placeholder for recycling views
    # path("sorting/record/", views.SortingCreateView.as_view(), name="sorting_record"),
]
