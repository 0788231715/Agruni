from django.urls import path
from . import views

app_name = "payments"

urlpatterns = [
    # Placeholder for payment views
    # path("invoice/<int:pk>/", views.InvoiceDetailView.as_view(), name="invoice_detail"),
]
